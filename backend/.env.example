PORT=4000
DATABASE_URL="file:./dev.db"
# Use a 64-char hex key, base64 32-byte key, or any strong passphrase.
ENCRYPTION_KEY="replace_with_secure_key"

# For GitHub Actions/hosted runs, use a hosted PostgreSQL URL as repository secret DATABASE_URL.
